# String.toLowerCase()

Get a lower-case version of a string.
As of 1.0, `toLowerCase()` modifies the String in place rather than returning a new one.

## Syntax:
```c++
myString.toLowerCase();
```
**Return** : Nothing.

